import "../styles/styles.css";
import React, { useMemo, useState, useRef, useCallback } from "react";

export default function Search() {

    return (
        <button className="Search">Feedback</button>
    );
  }